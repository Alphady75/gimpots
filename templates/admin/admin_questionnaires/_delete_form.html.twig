<form method="post" class="d-inline" action="{{ path('admin_questionnaires_delete', {'id': questionnaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questionnaire.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
